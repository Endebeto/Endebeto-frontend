/** Shared with Navbar (offset), MyBookings (query invalidation), and the banner. */
export const REVIEW_BANNER_HEIGHT_PX = 44;

export const MY_BOOKINGS_REVIEW_BANNER_QUERY_KEY = [
  "my-bookings",
  "review-banner",
] as const;
